# cn_SDN_mininet_project
SDN-based Multi-Switch Flow Table Analyzer using Mininet and Ryu Controller.  The project retrieves flow entries, monitors network traffic, and classifies rules as ACTIVE or UNUSED dynamically based on packet statistics.
